// lib/auth.ts
import { betterAuth } from "better-auth";

export const auth = betterAuth({
  // Minimal starter. Configure providers, email, DB, etc. as you grow.
  // See Better Auth docs for DB setup & providers.
});